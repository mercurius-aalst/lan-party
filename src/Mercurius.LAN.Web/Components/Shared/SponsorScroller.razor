<div class="wrapper">
    <div class="slider">
        <div class="slide">
            @foreach(var sponsor in _sponsors)
            {
                <img class="sponsor-logo" src="@sponsor.LogoUrl" alt="@sponsor.Name logo" />
            }
        </div>
        <div class="slide">
            @foreach(var sponsor in _sponsors)
            {
                <img class="sponsor-logo" src="@sponsor.LogoUrl" alt="@sponsor.Name logo" />
            }
        </div>
    </div>
</div>


